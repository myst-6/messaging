{
  "name": "conversations-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "lint": "next lint",
    "clean": "rm -rf .next && rm -rf out && rm -rf node_modules && pnpm i",
    "clean:win": "if exist .next rmdir /s /q .next && if exist out rmdir /s /q out && if exist node_modules rmdir /s /q node_modules && pnpm i",
    "deploy": "pnpm exec wrangler pages deploy out"
  },
  "dependencies": {
    "@chakra-ui/react": "^3.21.1",
    "hono": "^4.8.4",
    "next": "15.3.5",
    "next-themes": "^0.4.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@emotion/react": "^11.14.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5.5.2"
  }
}
